PromptMeGood Mobile Polish Command

To the AI assisting the user in Replit:
The core v3 chassis is in place, but the mobile layout (sub-768px) is currently too tight and the bottom bar is not fixed. Apply the following CSS updates to style.css to fix the mobile experience. Do NOT alter any HTML structure or JavaScript logic.

Step 1: Fix the Bottom Bar to the Viewport

The quick entry bar must stick to the bottom of the screen on mobile so it's always accessible.

Find the existing @media (max-width: 768px) block for .pmgv3-bottom (or add it if missing) and update it to:

CSS


@media (max-width: 768px) {
  .pmgv3-bottom {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 12px 16px;
    background: rgba(10, 34, 24, 0.95);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-top: 1px solid rgba(0, 200, 150, 0.2);
  }
}



Step 2: Add Scroll Clearance to the Body

Because the bottom bar is now fixed, the scrollable content needs extra padding at the bottom so the last elements aren't hidden behind the bar.

Update the mobile .pmgv3-body rule:

CSS


@media (max-width: 768px) {
  .pmgv3-body {
    grid-template-columns: 1fr;
    overflow-y: auto;
    padding: 12px;
    padding-bottom: 100px; /* Crucial: clears the fixed bottom bar */
    gap: 24px; /* Give sections more breathing room vertically */
  }
  
  /* Ensure columns don't have their own scrollbars on mobile, let the body scroll */
  .pmgv3-left, .pmgv3-right {
    overflow-y: visible;
    padding-right: 0; /* Remove desktop scrollbar padding */
  }
}



Step 3: Resize Tuning Pills for Mobile

The tuning pills are currently inheriting a min-height: 44px rule that makes them too bulky on small screens.

Add this specific override for the tuning pills on mobile:

CSS


@media (max-width: 768px) {
  .pmg-prompt-tuning .pmg-tune-pills {
    gap: 8px;
  }
  
  .pmg-prompt-tuning .pmg-tune-pill {
    min-height: 36px !important; /* Override the 44px rule */
    min-width: auto !important;
    padding: 6px 12px !important;
    font-size: 13px;
  }
}



Step 4: Refine Topbar and Tabs Spacing

Give the top navigation a bit more breathing room.

CSS


@media (max-width: 768px) {
  .pmgv3-topbar {
    padding: 0px 16px;
  }
  
  .pmgv3-tabs {
    padding: 0px 16px;
    gap: 8px;
  }
  
  .pmgv3-tab {
    padding: 6px 16px;
    font-size: 14px;
  }
}



Acceptance Criteria

1.
On a 390px screen, the bottom bar (with the Quick Entry input) is fixed to the bottom of the viewport.

2.
Scrolling to the very bottom of the page reveals all content (nothing is permanently hidden behind the bottom bar).

3.
The tuning pills are smaller and fit more comfortably on the screen without excessive wrapping.

4.
The overall layout feels less cramped, with better vertical spacing between sections.

