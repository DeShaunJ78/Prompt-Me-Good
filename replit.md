# Overview

This project, "PromptMeGood", is a pnpm workspace monorepo using TypeScript, designed to be a sophisticated AI prompt builder. Its core purpose is to provide a structured interface for users to craft precise prompts for AI, enhancing clarity and desired output. PromptMeGood aims to simplify and improve AI interaction through features like smart suggestions, auto-optimization, and quality checks, thereby increasing prompt effectiveness. It targets a broad user base with features like guided modes for beginners and an "Expert Mode" for advanced users. The project envisions a "Free" and future "PRO" tier, indicating a subscription-based revenue model, and aims to become a leading tool in the AI prompting space to foster better AI interactions and productivity.

# User Preferences

I prefer concise and direct communication. When making changes, prioritize iterative development and explain the high-level impact before diving into details. Please ask for confirmation before making any major architectural changes or introducing new external dependencies.

# System Architecture

## Monorepo Structure

The project is a pnpm workspace monorepo, with packages like `@workspace/api-spec`, `@workspace/db`, and `@workspace/api-server`.

## Tech Stack

-   **Monorepo Tool:** pnpm workspaces
-   **Node.js:** v24
-   **Package Manager:** pnpm
-   **TypeScript:** v5.9
-   **API Framework:** Express 5
-   **Database ORM:** Drizzle ORM
-   **Validation:** Zod and `drizzle-zod`
-   **API Codegen:** Orval (from OpenAPI spec)
-   **Build Tool:** esbuild (CJS bundle)

## PromptMeGood Artifact (`artifacts/promptmegood`)

PromptMeGood is a single-file static HTML AI prompt builder (`index.html`) built with vanilla JavaScript and Vite. It includes companion static pages: `guide.html`, `pricing.html`, `review.html`, and the standalone legal pages `privacy.html` and `terms.html` (footer links navigate to these directly; the original in-page legal modals were superseded). All companion pages are registered as Vite rollup inputs so they are emitted in production builds.

### UI/UX and Design Decisions

-   **Color Scheme:** CSS variables for theming, with a default teal palette.
-   **Interaction Feedback:** Improved responsiveness for touch devices with visual feedback on clicks.
-   **Responsive Design:** Adapts layout for mobile and desktop views.
-   **Form Layout:** Generate button sits immediately under the Goal field for one-click prompt creation. All tone/category/format/length/etc. settings are nested in a collapsible `<details id="settingsPanel">` panel that stays closed by default — Auto Optimize handles the picks for most users. Enabling Expert Mode auto-expands both the settings panel and the inner Advanced Options details.
-   **User Guidance:** Utilizes onboarding tours, modals, and toasts. The welcome tour modal is suppressed for return visitors via the `pmg_visited` localStorage flag (set on first dismiss/complete) so it only ever appears on a user's true first session.
-   **Theme Accent:** Footer-based picker with 5 swatches, persisted in `localStorage` and applied synchronously to avoid flash.
-   **Canonical Domain:** `https://www.promptmegood.com` with apex domain redirect to www for consistency.
-   **SEO:** Comprehensive meta-tags, optimized `<title>`, `<meta name="description">`, Open Graph (incl. `og:image`), Twitter Card tags (incl. `twitter:image`), and a `SoftwareApplication` JSON-LD block for rich Google results. Strategic heading hierarchy and font loading optimization.

### Key Features and Technical Implementations

-   **Prompt Builder:** Dynamic form with fields for goal, category, skill level, tone, output format, language, personality, details, guardrails, and max response length. Includes "Boost Toggles" in an "Advanced Options" section.
-   **Smart Systems:**
    -   **Smart Suggestions:** Keyword analysis to recommend prompt parameters.
    -   **Auto Optimize:** Applies suggestions to untouched fields, persisting user preferences.
    -   **AI Tool Recommender:** Suggests AI tools based on prompt goal keywords.
    -   **Prompt Strength Score:** A heuristic-based score with insights.
-   **Weekly Focus:** Rotating curated goal pin with persistence tracking.
-   **Guided Mode:** A structured modal to assist with prompt formulation.
-   **Refinement and Quality Check:** Features for prompt refinement, undo, and a "Quality Checker" providing heuristic-based suggestions.
-   **Prompt Sharing:** Encodes prompt parameters into a URL hash for shareable links.
-   **Expert Mode:** Opt-in mode hiding guidance and revealing advanced controls, with state persisted in `localStorage`.
-   **API Server Layout:** Cross-cutting middleware lives in `artifacts/api-server/src/middlewares/` — `rateLimit.ts` (sliding-window per-IP limiter factory plus the configured `generateLimiter` / `runLimiter` / `rateLimit` instances), `sanitize.ts` (`clampString`, `sanitizeGoal` with the `INJECTION_BLOCKLIST`), and `costGuard.ts` (daily $3 USD cap, two-stage `costCheck` middleware + explicit `chargeCost(endpoint)` helper, debounced disk flush to `data/pmg_cost.json`). `routes/ai.ts` imports from these modules and only retains the route handlers and the prompt-stats counter (which is a separate concern, persisted to `data/pmg_stats.json`).
-   **AI Routes (Backend):** `POST /api/generate` accepts both legacy `{prompt}` (Improve with AI) and structured `{goal, category, experience, tone, format, language, personality, extraDetails, avoid, moneyMode, humanVoice, clarityBoost, expertMode}` payloads. `POST /api/generate-stream` streams the same structured payload as Server-Sent Events (`data: {"text":"…"}` chunks ending with `data: [DONE]`). `POST /api/run` (NEW) takes `{prompt}` and streams the **gpt-4o** response back as SSE — this powers the in-app "Run With AI" feature so users can execute their finished prompt and see results without leaving the page. `GET /api/stats` returns `{promptCount, runCount}` (both from an in-memory counter with debounced disk-flush at `data/pmg_stats.json`). `GET /health` (mounted at app root for direct ops monitoring) and `GET /api/health` (the path the frontend keep-alive pings every 4 minutes, since the artifact path-router only forwards `/api/*` to this api-server) both return `{status:"ok"}`. The generate endpoints use `gpt-4o-mini` with `max_completion_tokens: 600`; `/api/run` uses `gpt-4o` with `max_completion_tokens: 1000` and a 2000-char prompt cap. Per-endpoint rate limits: `generateLimiter` 20/hr, `runLimiter` 5/hr, legacy `rateLimit` 10/min — all keyed by `req.ip` with `trust proxy = 1`. **Cost protection:** an in-memory `INJECTION_BLOCKLIST` rejects classic prompt-injection patterns (`ignore previous instructions`, etc.) on `/generate` and `/generate-stream`; a daily $3 USD cost cap (in-memory tally keyed by UTC date, 1-second debounced flush to `data/pmg_cost.json`) is enforced via a two-stage guard — `costCheck` middleware GATES requests, while `chargeCost(endpoint)` is called explicitly INSIDE each handler AFTER body validation, so malformed/oversized payloads cannot drain the budget. Per-call rates: `COST_PER_GENERATE = 0.004`, `COST_PER_RUN = 0.010`. Other endpoints (`/api/generate-prompt`, `/api/refine-prompt`, `/api/image-prompt`) use `gpt-5.4`. All AI calls go through a secure server-side client.
-   **AI Frontend Client:** `window.__pmgAI` provides `generateStream`, `generateStructured`, `generateRaw`, `generate`, `refine`, and `imagePrompt`. The Generate form uses the streaming-first fallback chain `stream → structured → raw → local template` so a single transient backend failure never erases the user's prompt. The Improve with AI button continues to call legacy `/api/generate {prompt}`. Per-month `localStorage` caps (100 generates, 50 refines, 10 image prompts) still apply, and limit-reached errors trigger a non-destructive toast plus the local fallback.
-   **Run With AI (Frontend):** `window.runWithAI` and `window.copyAIResponse` (exposed via the `__pmgRun` IIFE) power the post-generation "Run This Prompt With AI" panel. The `#runSection` is hidden by default and revealed by `finalize()` after a prompt is generated. Clicking Run reads the (editable) prompt from `#resultBox.textContent`, POSTs to `/api/run`, parses the SSE stream (with `[DONE]` hard-stop and CRLF normalization), and streams tokens into `#aiResponseOutput` live. Errors (network / 429 / model failure) render inline. A separate IIFE pings `/api/health` every 4 minutes (only while the tab is visible) to keep the dyno warm during active sessions.
-   **Hero Usage Counter:** A hidden `#usageCounter` element in the hero loads `/api/stats` on page load and reveals itself only when the COMBINED count (`promptCount + runCount`) exceeds 100. Failures are silent so the hero is never broken by a stats outage.
-   **Dev-Only Code Review Tool:** `POST /api/review` (mounted in `routes/index.ts` only when `NODE_ENV !== "production"`, with a defensive in-handler 403 fallback) bundles a curated list of source files (`app.ts`, `routes/index.ts`, `routes/ai.ts`, `routes/health.ts`, `routes/review.ts`, `index.html` — capped at 80KB/file and 400KB total) and streams a structured Claude review (claude-sonnet-4-6 via the Replit-managed Anthropic AI integration; no user-supplied API key) back as SSE. The companion page at `/review.html` (registered as a Vite rollup input alongside main/guide/pricing) lets a developer click "Run Review", watch the stream live, then see scored cards (bugs/UX/security/polish 0-10), a summary, and filterable issue cards with "Show fix" / "Copy fix" affordances. Marked `noindex,nofollow`. The endpoint and page never expose secrets or write data.
-   **Column Cleanup, Wider Layout, Real Cat Before/After Move (T33, NEW):** User feedback after T32: too much orphan/redundant chrome in the LEFT workspace column (the "Create, refine, and run your prompts here." subtitle, the "(N Of N Free Today)" daily-hint chip from `pmg-pro.js`, the original `#prompt-form > .field.field-primary` goal label+textarea+helper that duplicates T31's inline panel, the older `#guided-cta-row` "Help Me Start" row that duplicates T32's callout, the T24 "Recommended for the best results — guided in under a minute." `#pmg-hms-helper` microcopy, and `#weekly-goal-pin` which T32 had merely relocated). User also asked to (i) make both columns wider, (ii) loosen the cramped Photography Suite pill chips, and (iii) MOVE T28's in-column `#pmg-image-before-after` block (with its 📷/✨ emoji thumbs and "A Cat." vs "A Sleek Black Cat…" copy) UP into the existing static `#see-the-difference` `.std-grid` alongside the 3 text before/after rows, replacing the emoji thumbs with REAL same-cat photos (before = washed-out phone snapshot, after = cinematic golden-hour portrait of the SAME tabby). T33 (`pmgT33Cleanup`, final IIFE in `pmg-ux.js`) (a) injects CSS that hides `#workspace-header`, `.pmg-daily-hint`, `#prompt-form > .field.field-primary`, `#pmg-hms-helper`, `#guided-cta-row`, `.build-cta-guidance`, `#weekly-goal-pin`, and `#pmg-image-before-after` (DOM preserved so T31 can keep two-way binding to `#goal`, T30 can keep `click()`-ing `#guided-mode-btn`, and `pmg-pro.js`'s gated handlers stay attached to their buttons), (b) widens `#builder > .container.app-shell` to `1340px` with `!important` to override the global `1100px !important` cap from `index.html`, (c) loosens `#pmg-photo-suite .pmg-photo-group-body` gap to `10px` and tightens `.pmg-photo-pill` padding to `6px 12px` with `font-size: 13px` so the chips read as tags rather than crowded buttons, (d) builds a NEW `#pmg-t33-image-std-row` `.std-row` and appends it to `#see-the-difference .std-grid` containing two `.std-cell`s (`std-before` / `std-after`) each with the new `<img class="pmg-t33-cat-img">` (sourced from `public/images/pmg-cat-before.png` and `pmg-cat-after.png`, both AI-generated to depict the SAME fluffy gray-and-white green-eyed tabby in the SAME window-sill setting — before is intentionally bland phone-camera quality, after is cinematic 85mm golden hour) plus the existing `.std-cell-label` and `.std-cell-text`, (e) marks the relocated source via `data-pmg-t33-moved="1"` for traceability, and (f) attaches a brief `MutationObserver` retry so T28's late-mount of `#pmg-image-before-after` is captured (T28 builds the source AFTER T33 may have run; the observer retries the move once the source appears, then disconnects after 60s). Idempotent via `window.__pmgT33Init`. No backend / IDs / classes / JS variables changed; CSS variables only.
-   **Symmetric Help Me Start Callouts + Weekly Pin Move (T32):** User feedback after T31: the Help Me Start buttons need to be visually obvious primary buttons that immediately open the question flow, both columns must show "★ Most Loved" (previously only LEFT had it), and both should share an identical clean vertical structure with no guesswork: badge → title → description → big primary button → "or" divider → outlined secondary "I Know What I Want — Just Start Typing". User also asked to lift `#weekly-goal-pin` out of the buried builder form up to right under the column header. T32 (`pmgT32SymmetricCallouts`, final IIFE in `pmg-ux.js`) (a) edits T30 (one-line change at `openModal` definition) to expose `window.__pmgT30OpenImageWizard` so T32's rebuilt button can trigger the 5-step image wizard, (b) injects design tokens at `:root` (`--pmg-most-loved-bg`, `--pmg-most-loved-fg`, `--pmg-on-primary`, `--pmg-callout-divider`) so the rest of T32 only references CSS variables, (c) rewrites `#pmg-text-help-row` and `#pmg-image-help-row` innerHTML with the new vertical structure but KEEPS the same button ids (`#pmg-text-help-row-btn`, `#pmg-image-help-row-btn`, `#pmg-text-help-row-skip`, `#pmg-image-help-skip`) so T31's capture-phase skip-link toggle continues to match unchanged, (d) wires LEFT primary to click the existing `#guided-mode-btn` (the original 4-question modal) and RIGHT primary to call `window.__pmgT30OpenImageWizard()` with a fallback that opens `#pmg-t30-image-modal` directly if T30 hasn't loaded its export yet, (e) moves `#weekly-goal-pin` via `insertBefore` right after `#pmg-col-text-header` (it remains hidden by the existing `body.pmg-pre-gen .pmg-post-gen` gate until first generation, then surfaces at the top of the column instead of buried in the form), (f) tightens the T31 inline-typing panel positioning so it sits cleanly below the new vertical callout (overrides T31's `-10px` overlap with `8px` margin + full radius), (g) defensive `!important` on display/visibility/background/color for `.pmg-callout-primary` because a legacy rule in `index.html`'s inline `<style>` block was suppressing it at runtime, and (h) attaches a single throttled `MutationObserver` (rAF-coalesced, scoped to `#pmg-columns` if present) plus a 350ms × 40 retry that reapplies via the `data-pmg-t32` / `data-pmg-t32-moved` sentinel attributes if any later observer replaces a row or moves the pin back. Idempotent via `window.__pmgT32Init`. No ids/classes renamed; only existing CSS variables and four new T32 design tokens (no other literals).
-   **Per-Column Inline Typing Panel + Toggle Skip (T31):** User asked for a typing field "right under" each Help Me Start callout so users who want to skip the wizard get an immediate field instead of having to scroll. T31 (`pmgT31InlineTypingPanel`, IIFE in `pmg-ux.js`) (a) on each column lazy-builds a collapsible `.pmg-inline-typing` panel with its own `<textarea>` two-way bound (input/blur sync) to the canonical `#goal` so the existing builder pipeline is untouched, (b) the skip-link styled buttons (`#pmg-text-help-row-skip`, `#pmg-image-help-skip` from T30) are repurposed as toggle controls — text flips between "Just Start Typing ↓" and "Hide Text Field ↑" with `aria-expanded` mirrored — using a capture-phase listener so it intercepts T30's previous handler, (c) image-side panel routes its text into `#goal` and scrolls to `#builder-image` on focus to match T30's wizard outcome flow. Idempotent via `window.__pmgT31Init`.
-   **Image Prompt Wizard + Help Me Start Callouts (T30):** Built the parallel "Help Me Start" path for the image side. T30 (`pmgT30ImageWizard`, IIFE in `pmg-ux.js`) (a) adds a brand-new 5-step modal (`#pmg-t30-image-modal`: Subject → Style → Mood → Setting → Details) with chips per step plus a "custom" escape input, composing the chip choices into a sentence and dropping the result into `#goal`, then scrolling to `#builder-image` and surfacing a toast, (b) adds matching "Help Me Start" callouts to both columns at the top above the workspace, (c) tints both panels with a teal-derived gradient, (d) wires the hero CTAs to pulse the help-row after scroll. Both LEFT and RIGHT now share the same "answer a few questions and we'll build the prompt for you" entry point, with the LEFT side reusing the existing `#guided-mode-btn` modal and the RIGHT side using the new wizard.
-   **Hero Centering + Teal Accents + Photo Use Cases (T29):** Polish pass — centered the hero column, restored teal accent borders/glows on the two columns, and added two photo-prompt cards to the popular-uses grid (now 8 cards) so the "See Real Examples" use cases include image-prompt examples alongside text. `applyPopularUseCard` extended to recognize the new `data-photo-*` attributes and seed the photo subject/details when a photo card is clicked.
-   **Two-Column Build Area (T28, NEW):** The user defined the canonical UX for the home-page build area: keep two CTAs in the hero ("Create A Text Prompt" / "Create An Image Prompt") and split the build area into two equal columns operating linearly top-to-bottom — LEFT column = the entire text-prompt workflow (run with AI in-house OR copy/export), RIGHT column = the entire image-prompt workflow (generate here with DALL·E OR copy/export, with the Photography Suite included as a bonus refinement). The `.app-shell` element already had `display: grid; grid-template-columns: 1fr 1fr;` (line 527 of `index.html`) and currently held two direct children — `#builder-panel` (input form) and `#result-panel` ("Your Fixed Prompt") — so the original grid was being used to split the TEXT workflow into input/output halves rather than to pair text vs image. T28 (`pmgT28TwoColumnBuild`, final IIFE in `pmg-ux.js`) re-uses that grid by: (a) creating a `#pmg-col-text` wrapper in `#builder-panel`'s slot and moving BOTH `#builder-panel` and `#result-panel` inside it (preserving order and all event listeners — they're still selected by ID everywhere), (b) creating `#pmg-col-image` as the new sibling so it lands in grid column 2, (c) injecting matching column headers ("Workspace · Create A Text Prompt" / "Workspace · Create An Image Prompt"), (d) injecting `#pmg-image-help-row` (Help Me Start row that scrolls to the suite and auto-clicks `.pmg-photo-surprise` to seed picks), (e) injecting `#pmg-image-before-after` (a Before/After cards block showing "A Cat." vs a richly described version, using emoji thumbs as placeholders), (f) moving `#photo-suite-section` and `#pmg-image-generator-section` (built async by T23) into the right column via a `MutationObserver` retry that stops as soon as both targets are inside the column (or after 60s/`pagehide`), (g) reframing the hero CTAs so `#hero-build-cta` reads "Create A Text Prompt", a new outlined-primary `#hero-image-cta` is inserted next to it linking to `#builder-image`, and `#hero-usecases-cta` is relabeled "See Real Examples / Text Prompts And Image Prompts". On mobile (<900px) the existing `.app-shell` media rule already collapses to one column; T28 adds matching mobile rules so the help row stacks and the Before/After grid becomes single-column. T23's body observer only checks for the EXISTENCE of those two sections (not their parent) so it does not fight the move. Idempotent via `window.__pmgT28Init`. No IDs/classes renamed; CSS variables only; no flex/order tricks.
-   **Unified Photo Flow (T27, NEW):** The user pointed out that "Photography Suite" and "Create An Image" were two separate UI surfaces for what is conceptually the same workflow with two different outcomes. The final IIFE in `pmg-ux.js` (T27 `pmgT27UnifyPhotoFlow`) consolidates them: (a) hides the redundant top-of-builder mode-switch tabs (`#modeSwitch`, `.image-mode-hint`) and the standalone `#image-generate-btn` via CSS `!important`, (b) hides the legacy image-mode-only photo widgets that earlier phases injected into the top builder (`.pmg-photo-assistant`, `#pmg-photo-accordion`, `#pmg-build-image-btn`) since the suite now duplicates them, (c) forces `body.image-mode` to be cleared at startup so returning visitors land in the canonical text-prompt builder, (d) injects a new `📋 Copy Prompt` outcome button (class `pmg-photo-copy`) into `.pmg-photo-actions` immediately before the existing send button, (e) reframes the existing send button label to `🎨 Generate Image Here` (its `.pmg-photo-send` class is unchanged so T23's click handler still fires), (f) inserts a `What Do You Want To Do?` outcome label above the action row, and (g) mirrors the disabled state from the send button onto the copy button via a `MutationObserver` on the `disabled` attribute (throttled by T26's ObserverGuard). Copy button writes `subject — photoText.` to the clipboard via `navigator.clipboard.writeText` with an `execCommand('copy')` fallback, then displays the existing T23 toast with success feedback. No IDs, classes, or JS variable names from earlier phases were renamed; the in-page guide step at `index.html:3922` was updated to reference the two outcomes. Idempotent via `window.__pmgT27Init`.
-   **Renderer Stability (T26 ObserverGuard):** The home page bundles 12 stacked UX phases (T14–T25) inside `pmg-ux.js` (~264KB) which collectively install ~30 `MutationObserver` instances, 17 of them watching `document.body` with `subtree: true`. On low-powered devices this overwhelmed the renderer and could crash the tab. Mitigations: (1) `pmg-ux.js` is now loaded with `defer` so it no longer blocks parsing, and (2) the very first IIFE in `pmg-ux.js` (T26 ObserverGuard) monkey-patches `window.MutationObserver` to coalesce delivered records via `requestAnimationFrame` (one user-callback invocation per frame, in order) and to auto-disconnect any single observer that exceeds 5,000 records in a rolling 1s window (runaway protection with a `console.warn`). The wrapper preserves native semantics: callbacks are invoked with `this` bound to the observer instance, and `instance.disconnect()` / `instance.takeRecords()` are wrapped to drop or drain the coalesced queue exactly like the native API. Idempotent via `window.__pmgT26ObserverGuard`. Companion pages (pricing/privacy/terms/guide) were not affected and are unchanged.
-   **Image Generation UX:** `pmg-image-fix.js` (loaded after pmg-fixes-v6.js) layers UX over the existing `runImageGeneration()` flow without renaming any IDs. Adds: (1) a live progress pill `#pmg-image-progress` with a per-second elapsed counter and rotating reassurance copy during the 15–25s OpenAI image API wait, (2) a green success callout `#pmg-image-success` above the download button, (3) restyled prominent download button (`⬇ Save Image To Your Device`, full-width on mobile, brief pulse, dynamic timestamped filename), (4) a `#pmg-image-retry` button on errors, (5) a MutationObserver on `#imageResultWrap` that resets the generate-button label the moment an `<img>` arrives (fixes a v6 bug where the CTA stayed stuck on "Creating Your Image With DALL·E 3..." indefinitely because v6 only watched `#result-panel`). Idempotency-guarded via `data-pmg-image-fix-bound`; cleans timers on `pagehide`/`beforeunload`.
-   **Use Demo Values UX:** Button (`#fill-demo`) to load sample prompts, with visual feedback, loading states, and inline guidance.
-   **Smart Assist:** Inactivity-driven helper providing guidance, with debounced activity timers and suppression logic to prevent interference.

# External Dependencies

-   **PostgreSQL:** Primary database.
-   **OpenAPI Specification:** For API definition.
-   **Vite:** Frontend build tool.
-   **Zod:** Schema validation.