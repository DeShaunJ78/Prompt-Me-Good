# Overview

PromptMeGood is a pnpm workspace monorepo project focused on building an AI prompt builder. Its primary goal is to provide an intuitive interface for crafting effective AI prompts through smart suggestions, auto-optimization, and quality checks. The project aims to enhance AI interactions and productivity for users, offering "Free" and "PRO" tiers, with the ultimate ambition of becoming a leading tool in AI prompt engineering.

# User Preferences

I prefer concise and direct communication. When making changes, prioritize iterative development and explain the high-level impact before diving into details. Please ask for confirmation before making any major architectural changes or introducing new external dependencies.

# System Architecture

## Monorepo Structure

The project utilizes a pnpm workspace monorepo, organizing packages such as `@workspace/api-spec`, `@workspace/db`, and `@workspace/api-server`.

## Tech Stack

-   **Monorepo Tool:** pnpm workspaces
-   **Node.js:** v24
-   **Package Manager:** pnpm
-   **TypeScript:** v5.9
-   **API Framework:** Express 5
-   **Database ORM:** Drizzle ORM
-   **Validation:** Zod and `drizzle-zod`
-   **API Codegen:** Orval (from OpenAPI spec)
-   **Build Tool:** esbuild (CJS bundle)

## PromptMeGood Artifact (`artifacts/promptmegood`)

PromptMeGood is a static HTML AI prompt builder (`index.html`) using vanilla JavaScript and Vite, complemented by static pages for guides, pricing, reviews, privacy, and terms.

### UI/UX and Design Decisions

-   **Color Scheme:** CSS variables for theming with a default teal palette.
-   **Responsive Design:** Adapts layout for mobile and desktop.
-   **Form Layout:** Specific placement for the "Generate" button and an always-visible Prompt Tuning section.
-   **Text Prompt Builder:** Features a linear column flow for prompt creation: goal textarea → optional file upload → always-visible Prompt Tuning section (Step 1.5) → "Fix My Prompt" action. The result panel includes empty, loading, and error states.
-   **Prompt Tuning Section:** First-class always-visible `<section id="settingsPanel">` styled with the same stack-card chrome as the Photography Suite Vibe Controls. Houses category, tone, output format, personality, max length, language, extra details, and avoid-list controls.
-   **Text Builder Sibling Flow:** The "Create A Text Prompt" column visually aligns with the Photography Suite using stack-card-style step headers.
-   **Keyboard Shortcuts:** A discoverable cheatsheet for global, builder, and photo suite shortcuts, triggered by a floating "?" button.
-   **User Guidance:** Onboarding flows, modals, toasts, and "Expert Mode."
-   **Canonical Domain & SEO:** `https://www.promptmegood.com` with comprehensive meta-tags.
-   **Two-Column Build Area:** Dedicated areas for text and image prompts.
-   **Image Prompt Wizard:** A 5-step modal for creating image prompts.
-   **Accessibility:** Fully opaque topbar and clear visual cues for interactive elements.
-   **Universal Close Affordances:** Modals, overlays, panels, and toasts can be closed via multiple methods.

### Key Features and Technical Implementations

-   **Prompt Builder:** Dynamic form for prompt parameters (goal, category, tone, output format) and "Boost Toggles."
-   **Smart Systems:** Includes Smart Suggestions, Auto Optimize, AI Tool Recommender, and Prompt Strength Score.
-   **Guided Mode:** Structured modal for prompt formulation.
-   **Refinement and Quality Check:** Features for prompt refinement, undo, and a "Quality Checker."
-   **Prompt Sharing:** Encodes prompt parameters into a URL hash.
-   **Expert Mode:** Opt-in mode revealing advanced controls, with state persisted in `localStorage`.
-   **API Server Layout:** Cross-cutting middleware for rate limiting, sanitization, and cost guarding.
-   **AI Routes (Backend):** Includes `POST /api/generate` (legacy/structured payloads, `gpt-4o-mini`), `POST /api/generate-stream` (SSE for structured payloads), `POST /api/run` (SSE for `gpt-4o` responses), and `GET /api/stats`.
-   **Rate Limiting:** Per-IP rate limits for generate (20/hr) and run (5/hr).
-   **Cost Protection:** In-memory `INJECTION_BLOCKLIST` and a daily $3 USD cost cap.
-   **AI Frontend Client:** `window.__pmgAI` provides generate, refine, and image prompt functions with streaming-first fallback. `localStorage` caps apply per month.
-   **Run With AI (Frontend):** Post-generation panel utilizing `gpt-4o` responses.
-   **Hero Usage Counter:** Displays combined prompt and run counts when exceeding 100.
-   **Image Upload + Vision Analyze:** Allows uploading images for AI analysis via `/api/analyze` to describe content and fill the prompt textarea.
-   **Paywall Switch (Open-Beta):** Central helper `isPaywallActive()` controlled by Replit Secrets for beta access and paywall activation.
-   **Prompt Tuning Panel:** Always-visible advanced settings (category, tone, output format, personality, max length, language, extra details, avoid list) styled to mirror the Photography Suite Vibe Controls. Replaces the legacy collapsible "More Control" chip.
-   **Supabase Auth + Save Best Prompts:** Magic-link email authentication for users to save and reload prompts and AI outputs.
-   **Renderer Stability:** `T26 ObserverGuard` monkey-patches `window.MutationObserver` for stability.
-   **Image Generation UX:** Polished waiting card during the ~10–25s wait — three staggered pulsing dots, phased status messages that crossfade between stages ("Reading your prompt…" → "Sketching the composition…" → … → "Adding finishing touches…" → "Almost ready…"), an elapsed-seconds chip, and an asymptotic progress bar capped at 94% (snapped to 100% on completion). Plus success callout, restyled download button, and retry on errors. Honors `prefers-reduced-motion` (no pulse, no crossfade, no animated fill) and announces phase changes — not the per-second tick — to screen readers via a polite live region.
-   **Photography Suite Saved Combos (My Combos):** Power users can save the current pill selection across all five Photography Suite groups under a custom name. Saved combos persist in `localStorage` (`pmg.photo.savedCombos`, raw pill values), appear above the auto-tracked Recent row, and can be re-applied with one click or removed with a × button. Save This Combo button sits next to Surprise Me / Clear Picks and is disabled while no pills are active.
-   **Photography Suite Pin Surprise:** After Surprise Me runs, a transient "Pin This Surprise" CTA appears next to Surprise Me. Clicking it persists the random pill set into the same Recent row used by preset combos under an auto-generated label (e.g. "Surprise: Cinematic, 35mm Wide, Golden Hour, Rule Of Thirds…"). The Recent row storage (`pmg.photo.recentPresets`) now uses a tagged shape that supports both preset combos (`{kind:'preset', entries:[{group,idx},...]}`) and raw pill combos (`{kind:'raw', label, picks}`), with backwards compatibility for the legacy bare-array format. The Pin CTA auto-dismisses on any pill change, preset apply, clear, saved-combo apply, or send.
-   **Recent Combo Preview Tooltip:** Reuses the Quick-Style preset preview tooltip (`#pmg-photo-preset-tooltip`, Task #34) on the Recent Combos row so users can see what a chip will activate before tapping. Hover, keyboard-focus, or a ~700ms touch long-press on any `.pmg-photo-recent-btn` shows one chip per pill, prefixed with the group label from the GROUPS catalog (e.g. "Style: Cinematic", "Lighting & Mood: Golden Hour", "Lighting & Mood: Natural Window Light"), ordered by GROUPS so the layout is stable. Implemented by extracting per-button hover/focus/touch wiring into `wirePreviewHover(btn)` (shared with preset buttons), routing the recent-button case through `presetPreviewLabels` → `recentComboPreviewLabels`, and re-attaching handlers from `renderRecentRow()` after each row re-render. The delegated click on `#pmg-photo-recent` honors `presetTooltipState.suppressClick` so a touch long-press cannot accidentally apply the combo. One-tap apply behavior is unchanged. Cache buster: `pmg-ux.js?v=task43-1`.
-   **Smart Assist:** Inactivity-driven helper providing guidance.
-   **Storage Write Warning Banner:** When `saveHistory()` in `artifacts/promptmegood/index.html` cannot persist to `localStorage` (private mode, quota exceeded, or storage blocked), it surfaces an inline dismissible banner above `#history-list` styled in the same red family as `.pmg-history-error-card`. The CSS, DOM scaffolding (`#pmg-history-write-warning` with `role="status"`, `aria-live="polite"`), and `showWriteError(msg)` / `hideWriteError()` helpers live in `artifacts/promptmegood/public/scripts/pmg-history-states.js` (cache-busted as `?v=3-task42`) and are exposed on `window.__pmgHistoryStates`. Quota errors message "We're out of room…" while generic blocked storage shows "Your browser isn't letting us save…". The banner clears the moment a save succeeds, and re-shows on every subsequent failed save after a manual dismiss. Honors `prefers-reduced-motion` (no slide-in animation).
-   **Text Builder Live Feedback:** The "Create A Text Prompt" mode shows a live-feedback panel (`#pmg-tf-feedback`) anchored above the action row in `#tour-step-generate`. It surfaces (1) a confidence meter with three bands — "Too vague" (<35), "Getting there" (35–69), "Specific enough" (≥70) — driven by a small additive heuristic over goal length, presence of details/rules/max length, personality, and non-default category/tone/format/skill picks (minus 4 per vague word, capped at -16); (2) a `chars · ~tokens` estimate (chars/4) with a soft `· long` warning past 3,000 tokens; (3) a vague-word linter that scans goal+details+rules in one regex pass and renders one chip per unique word with a stronger-word suggestion (`good`→`effective/high-quality/measurable`, `stuff`→`materials/inputs`, `things`→`items/components`, plus 13 more); and (4) a collapsible "Live Preview Of Your Prompt" `<details>` that mirrors the assembled prompt as the user types/toggles, persisted via `localStorage` (`pmg.textfeedback.preview.collapsed`). All updates are debounced 150ms and announced via `aria-live="polite"`. The feature only renders when `body.pmg-text-sibling` is set (text-builder mode); a body-class MutationObserver tracks mode changes but only fires when `BODY_CLASS` actually toggles, so unrelated body-class churn from sticky bars/tour scripts cannot reset the debounce timer (this was the bug found in Task #48 — the feedback panel was correctly attached but the renderer never settled until that filter was added). The exact assembler is reused via `window.__pmgText = { getFormData, generatePrompt }`, exposed inline near the original IIFE in `index.html`. Disable hatch: `?notextfeedback` query string or `localStorage.setItem('pmg_textfeedback_disable','1')`. Cache buster: `pmg-text-feedback.js?v=task48-5`.
-   **Fix My Prompt Diff (Side-By-Side):** The Fix My Prompt action now opens a Google-Docs-style diff panel inside the result panel (`#pmg-fix-diff`, rendered after `#resultBox`) so users can review every structural change individually. The left pane shows the user's original raw goal; the right pane lists each suggested edit (role line, clarifying directives, category focus, tone, format, examples, next-actions, max-length, rules, personality modes, etc.) with a one-line plain-English reason ("Sets the AI's role…", "Avoid filler so the AI gets to the point…"). Each row has its own checkbox plus Accept/Reject buttons; "Accept All" / "Reject All" / "Re-apply" sit above. The result box is rewritten via `__pmgText.setPromptText` from the kept edits only, and a bottom summary recap shows pill counts ("X of Y edits applied"). The diff is rebuilt deterministically from form data via `window.__pmgText.buildEdits(data)`, dispatched from `finalize()` on the `pmg:builder-finalized` CustomEvent. A MutationObserver on `#resultBox` flags the diff stale if a refine/manual-edit overwrites it, and an immediate AI-divergence check marks stale on first render when the AI's free-form rewrite landed in the box before the diff initialized. Click handlers read state from `root.__pmgState` so a second Fix My Prompt run replaces the model cleanly without stale-closure corruption. Touch targets bumped to 36px (buttons) and 22px (checkbox) for mobile. Escape hatches: `?nofixdiff` query string or `localStorage.pmg_fixdiff_disable=1`. Cache buster: `pmg-fix-diff.js?v=task49-2`.
-   **Improve Your Prompt: History + Hover Preview (Task #50):** The "Refine Your Prompt" panel (`#improve-block`) gains a horizontal in-memory history strip that captures the previous prompt every time a quick-tweak runs (More Detailed, More Bold & Direct, Beginner Friendly, Improve With AI, or Fine-Tune Update Prompt). Each chip shows a short label + relative time and one is marked `aria-current="true"`. Clicking any chip calls `window.__pmgText.setPromptText(entry.text)` to restore that version (toast: "Restored: …"); the current state is also a chip so users can return forward. Capped at 14 entries; cleared on each fresh Fix My Prompt via the `pmg:builder-finalized` event. Quick-tweak buttons (`[data-remix]`) also gain a hover/focus preview popover (`#pmg-imphist-preview`, `position:fixed`, flips above when out of vertical room) that calls `window.__pmgText.generatePrompt(data, kind)` and shows a 360-char excerpt without committing — Escape, scroll, resize, blur, and mouseleave dismiss. Roving-tabindex keyboard nav on the strip: Arrow Left/Right, Home/End, Enter/Space to restore. A single MutationObserver on `#resultBox` is the source of truth for "prompt changed" so AI-improve, fine-tune, undo, and external scripts are all picked up; pendingTweak metadata is set in capture-phase click listeners so the observer knows what label to attach. Dedupe on identical text moves the current pointer instead of creating a duplicate chip. In-memory only (no localStorage). Test surface: `window.__pmgImproveHistory.snapshot()` / `.restore(i)`. Escape hatches: `?noimprovehistory` query string or `localStorage.pmg_improvehistory_disable=1`. Cache buster: `pmg-improve-history.js?v=task50-1`.
-   **Image Generator Variations + Compare + Use-Style Handoff (Task #52):** Three image-side enhancements layered on top of the existing Image Generator without renaming any IDs or breaking the single-image path. Wired by `artifacts/promptmegood/public/scripts/pmg-image-extras.js` (cache buster `task52-1`) and a small server change in `artifacts/api-server/src/routes/ai.ts`. **(1) Generate 4 Variations:** new `#pmg-imgx-variations-btn` in the image action row fires one POST `/api/image` with `{prompt, n: 4}`. Server now accepts optional integer `n` (clamped 1–4); when `n>1` it sets `quality:"low"` on `gpt-image-1` for fast/cheap variants and charges `chargeImage()` once per returned URL; response is `{ok, success, url, urls, enhancedPrompt}` with `url = urls[0]` for back-compat. The client renders a 2×2 thumbnail grid in `#imageResultWrap` (`role="list"`, each thumb a `<button>` for keyboard nav). Click a thumb → upscales to full size, primes the existing `#imageDownloadBtn`, leaves a "▦ Show All 4 Variants" link so the user can revisit the grid without re-paying for another /api/image call. The variants are cached in `state.variantUrls` until the next single-image regen wipes them. Variations only consume one `imageLimiter` slot (3/hr per IP) since it's one HTTP call. **(2) Before/After slider:** wraps `window.runImageGeneration` to capture the about-to-be-overwritten image src into `state.previousImageUrl` *before* the regenerator runs, then samples the new src ~60ms after it resolves. When both URLs exist, `#pmg-imgx-compare-btn` unhides; clicking mounts a `clip-path: inset(0 X% 0 0)` slider over the wrap (previous img stays full-bleed, new img is clipped). The handle supports mouse, touch (`touchstart`/`touchmove` with `passive:false`), and keyboard (`ArrowLeft`/`ArrowRight` 5%, Shift = 10%, `Home`/`End` = 0/100); the knob has `role="slider"` + `aria-valuenow`. Close button or ESC restores the original wrap content (stashed in a hidden `.pmg-imgx-cmp-stash` div). Re-clicking the Compare button toggles. Regenerating closes any open slider. **(3) Use This Style On A New Photo:** `#pmg-imgx-style-btn` (visible after any image is generated) clears `#goal`, scrolls + focuses it, and shows a `polite` aria-live banner `#pmg-imgx-handoff` above the result section reminding the user that the style pills are still selected. We don't touch `.is-active` pill state — the existing Photo Suite DOM is the single source of truth. The banner auto-hides after the next successful regeneration. Test surface: `window.__pmgImageExtras = { state, openSlider, closeSlider, upscaleVariant, _setLast, _setPrev, _setVariants }`. Escape hatches: `?noimageextras` query string or `localStorage.pmg_imageextras_disable=1`. Visibility-controlled buttons sync via `syncButtonVisibility()` on every state mutation. Idempotent boot via `window.__pmgImageExtrasLoaded` and `actions.dataset.pmgImgxButtons`.
-   **Photography Suite Preset Thumbnails (Task #51):** Every Quick-Style preset button (`.pmg-photo-preset`) across all five Photography Suite groups — Style, Camera & Lens, Lighting & Mood, Composition, Color Palette (4 presets per group, 20 total) — now renders a small (18×18, shrinks to 16×16 on ≤420px viewports) representative thumbnail to the left of its text label so users can recognise the preset at a glance. Thumbnails are 20 standalone static SVG files served from `artifacts/promptmegood/public/images/presets/` (e.g. `style-cinematic.svg`, `camera-portrait-lens.svg`, `palette-warm-sunset.svg`); the JS module `artifacts/promptmegood/public/scripts/pmg-preset-thumbs.js` injects an `<img loading="lazy" decoding="async" alt="…">` per preset so the browser handles the deferred fetch natively. Each `<img>` carries descriptive alt text (e.g. *"Cinematic style thumbnail: dark widescreen frame with letterbox bars and a lens flare highlight."*) so screen-reader users get the same visual hint as sighted users; the button's existing aria-label is unchanged. The `THUMBS` manifest in the JS is keyed by `[group][presetIndex]` and **must stay in sync with `PRESETS` order in `pmg-ux.js`** — adding or reordering a preset there requires both a manifest entry and a matching SVG file here. A MutationObserver on `body` waits for `#pmg-photo-suite` to mount (pmg-ux.js renders it after init), then a second MutationObserver on the suite catches any subsequent re-renders. Idempotent via `data-pmg-thumb="1"` flag. Defensive `error` handler on every `<img>` hides the wrapper if the SVG ever 404s (no broken-image glyph). Hover scales the thumb 1.08× and tints its ring with `--color-primary`; active preset gets a 1.5px primary ring; dark mode adds a 1px white-tint ring so light-themed thumbs (Editorial, Minimalist, Overcast) don't disappear; `prefers-reduced-motion` drops the transform but keeps the ring change. Wrapped in a try/catch so any failure logs and exits without breaking the suite. Test surface: `window.__pmgPresetThumbs.countInjected()` / `.countTotal()`. Escape hatches: `?nopresetthumbs` query string or `localStorage.pmg_presetthumbs_disable=1`. Cache buster: `pmg-preset-thumbs.js?v=task51-2`.
-   **Smarter Vault: search, multi-tag AND, sort persistence, Compare Many (Task #54):** The Vault (`#history` section in `artifacts/promptmegood/index.html`) gains four upgrades layered on the existing list. **(1) Multi-tag chip filters:** the previous single-tag filter (`historyTagFilter` string) is replaced by `historyTagFilters` (`Set`) so the tag bar (`#history-tag-bar`), the in-item tag pills (`[data-history-action="filter-tag"]`), and the global-search "tag" results all toggle membership instead of clobbering it; chips render `aria-pressed` per-tag and the empty-state copy describes the active combo (e.g. `#food + #easy`). The "All" chip (empty `data-tag-filter`) clears the entire set. Filter logic in `renderHistory` rejects any item missing **any** active tag (true AND). Helpers: `toggleHistoryTagFilter(tag)`, `setHistoryTagFilterSingle(tag)` (used by global search to jump straight to one tag), `clearHistoryTagFilter()`. Editing an item's tags prunes any active filter that no other item carries. **(2) Four sort modes** (`favorites-first` / `newest` / `oldest` / `most-used`) persisted to `localStorage["promptmegood:vaultSort:v1"]` via `loadVaultSort()` / `saveVaultSort()` with whitelist (`VALID_SORTS`) + safe fallback to `favorites-first`. The `<select id="history-sort">` is hydrated with the stored value before the first render and writes back on every `change`. Only `favorites-first` honors the manual favorite drag-reorder map (`favOrderMap`); the other three modes ignore favorite pinning so chronological/usage sorts are strict. **(3) Compare Many (2–5 prompts):** the legacy "Compare Two" toggle is renamed to "Compare" (`#compare-two`); the banner now reads "Pick 2–5 prompts to compare side-by-side." and a new `#compare-launch` "Compare Now" button (disabled by default) lights up at 2 picks and shows the live count. Constants `COMPARE_MIN=2` / `COMPARE_MAX=5`. `handleComparePick` now toggles selection and toasts when capped. `openCompareMany(items)` keeps the static two `.compare-col` children in the modal but hides them via inline `display:none` and appends N `.compare-col-many` siblings (one per picked item), labels `escapeHtml`'d, body via `textContent` (no XSS). Each many-mode column has Copy + Use This buttons wired through a single delegated `compareManyClick` handler. `tearDownCompareMany()` removes the dynamic columns, restores the legacy children, drops the listener, and clears state — called from both `closeCompare` and the legacy `openCompare` so the 2-prompt remix flow (More Detailed / Bold & Direct / Beginner Friendly) is unaffected and reuses the same modal. CSS `.compare-grid.is-many { display:grid; grid-auto-flow:column; grid-auto-columns: minmax(280px, 1fr); overflow-x:auto; }` gives horizontal scroll on small viewports. **(4) Tests:** `artifacts/promptmegood/tests/vault-smart.spec.ts` (6 specs, all in the `mobile-360` Playwright project): full-text search filtering, multi-tag AND combination + "All" clear, sort persistence across reload (most-used), oldest sort ordering, Compare Now disabled until 2 picks then opening N columns with correct teardown, and the 5-pick cap. Out of scope (deferred to a future task): folders/nested organization and user-defined commands (Task #53 follow-up).
-   **Command Palette (⌘K, Task #53):** Global keyboard-driven palette opened with **⌘K** on macOS or **Ctrl+K** on Windows/Linux from anywhere in the app — works even while focused inside an `<input>` / `<textarea>` (the palette is the documented escape hatch from focused fields). A centered overlay (`#pmg-cmdk-backdrop`, z-index 220, above the shortcuts cheatsheet) renders a search input plus a fuzzy-scored, grouped result list. Sources indexed each open: **Modes** (Switch To Write Mode, Switch To Image Mode), **Actions** (Fix My Prompt, Generate Image, Improve With AI, Make It More Detailed / Bold & Direct / Beginner Friendly, Surprise Me — Photo Suite, Surprise Me — Text Builder, Save This Combo, Copy Result, Copy Shareable Link, Run With AI, Keyboard Shortcuts) — each gated by a `visible()` predicate so only currently-rendered/enabled buttons appear, **Preset Groups** (Style, Camera & Lens, Lighting & Mood, Composition, Color Palette — execute by expanding the group if collapsed and scrolling its head into view), **My Combos** (`pmg.photo.savedCombos` — clicks the matching `.pmg-photo-saved-btn[data-saved-index="N"]`), **Recent Combos** (`pmg.photo.recentPresets` — clicks `.pmg-photo-recent-btn[data-recent-index="N"]`, capped to whatever the Recent row exposes), and **Vault** (`promptmegood:history:v1`, capped at 100; restoring a vault item switches to write mode if needed, clears `#history-search`, scrolls `#history-list` into view, and clicks the matching `.history-item[data-id="…"] [data-history-action="restore"]` in a 3-attempt retry loop to absorb re-render latency). Fuzzy scorer is in-script (no dependency): exact title-prefix > exact title-substring > exact blob-substring > subsequence with consecutive-run + word-boundary bonuses; queries with no matched chars are dropped. **Keyboard wiring:** ⌘/Ctrl+K toggles (capture-phase, preventDefault to suppress Chrome's omnibar focus); inside the palette ↓/↑ move (Home/End jump), Enter executes (palette closes first, command runs in a `setTimeout(0)` so it sees the post-close focus/scroll), Esc closes, Tab is trapped to the input. Backdrop click closes (target===backdrop guard). Focus is restored to whatever opened the palette via a captured `lastFocus` reference. The Keyboard Shortcuts cheatsheet (`pmg-shortcuts.js`, bumped to `?v=9-task53`) now lists ⌘K / Ctrl+K (platform-detected) at the top of the **Global** group with the label "Open Command Palette (Primary Entry Point)". Test surface: `window.__pmgCommandPalette = { open, close, toggle, isOpen, _getCommands(), _query(q) }` (the latter two are read-only snapshots — they never invoke `run()`). Idempotent boot via `window.__pmgCommandPaletteLoaded`; honors `prefers-reduced-motion` (no fade/slide). Escape hatches: `?nocmdk` query string, `localStorage.pmg_cmdpalette_disable=1`, or the legacy master kill `localStorage.pmg_disable=1`. Cache buster: `pmg-command-palette.js?v=task53-5`. **Cross-mode discoverability (v=task53-5):** all mode-scoped commands — Generate Image, Photo-Suite Surprise Me, Save This Combo, all 5 preset groups, all saved combos, all recent combos (image-side) and Fix My Prompt, Improve With AI, the 3 remix buttons, Text-Builder Surprise Me (write-side) — are now globally discoverable from any mode. Their `visible()` predicate is `availableInMode(selector)` (true if either the target is rendered OR `window.setMode` exists), and their `run()` is wrapped in `actInMode(targetMode, innerRun)` which switches modes first via `ensureMode()` (clicks `imageModeBtn`/`writeModeBtn` or calls `window.setMode()`) and then dispatches the inner action through a 6×80ms retry window to absorb destination-UI mount latency. When already in the right mode, behavior is unchanged. Cross-mode entry points (mode switches, Keyboard Shortcuts, vault restore) continue to use API-surface-first visibility for marketing-splash availability. **Smoke test:** `tests/command-palette-smoke.spec.ts` (8 specs) covers global hotkey + overlay + focus, BOTH modifier variants via synthetic events, the `_getCommands` / `_query` test surface, Enter execution, ArrowDown navigation (`.is-active` single-selection invariant), cross-mode discoverability of all 5 preset groups + image-side actions from the write-mode marketing splash, and catalog sanity (≥15 entries, no duplicate ids) — runs alongside `horizontal-overflow.spec.ts` in the `overflow-360` Playwright project. Out of scope (deferred): user-defined custom commands.
-   **Mobile Overflow Guard (Playwright):** `artifacts/promptmegood/tests/horizontal-overflow.spec.ts` (run via `pnpm --filter @workspace/promptmegood run test:overflow`, registered as the `overflow-360` validation command) loads the homepage at a 360×800 viewport against the running dev server (`http://localhost:80`, override with `PMG_BASE_URL`) and asserts (1) `documentElement.scrollWidth <= viewport + 1px tolerance`, and (2) neither `<html>` nor `<body>` uses `overflow-x:hidden` as a band-aid. On failure it walks the DOM and prints up to 25 visible elements whose right edge exceeds the viewport (skipping anything clipped by an ancestor with `overflow:hidden|clip|scroll|auto`), with selector, computed right edge, width, and a text snippet so the next agent can fix the regression at source.

# External Dependencies

-   **PostgreSQL:** Primary database.
-   **OpenAPI Specification:** For API definition.
-   **Vite:** Frontend build tool.
-   **Zod:** Schema validation.
-   **Stripe:** Payment processing for subscriptions.
-   **Supabase:** Authentication and database for user-saved prompts.